aws:
  key: dummy-key
  secret: dummy-secret
  region: "dummy-region-with-quotation-marks"

callback:
  max_retries: 3
  retry_interval: 5
  url: "dummy-domain.com"

pipeline:
  interval: 3

smtp:
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
