attachments:
  base-path: "/base/attachments/path"

callback:
  max_retries: 3
  retry_interval: 5
  url: "dummy-domain.com"

eml-storage:
  path: "/efs/eml"

health-check:
  server:
    port: 8080

mysql:
  host: "localhost"
  port: 3306
  user: "root"
  password: "test"
  database: "mailculator_test"

pipeline:
  interval: 3

pipelines:
  mysql:
    enabled: true

smtp:
  host: dummy-host
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
