aws:
  base_endpoint: "${TEST_ENV_VAR}"

callback:
  max_retries: 3
  retry_interval: 5
  url: "dummy-domain.com"

health-check:
  server:
    port: 8080

pipeline:
  interval: 3

smtp:
  host: dummy-host
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
