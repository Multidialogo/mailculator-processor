aws:
  key: dummy-key
  secret: dummy-secret
  region: "dummy-region-with-quotation-marks"

pipeline:
  interval: 3
  callback_url: "dummy-domain.com"

smtp:
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
