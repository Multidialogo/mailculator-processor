aws:
  key: dummy-key
  secret: "${TEST_ENV_VAR}"
  region: "dummy-region-with-quotation-marks"
  base_endpoint: dummy-base-endpoint

callbacks:
  max_retries: 3
  retry_interval: 5

smtp:
  host: dummy-host
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
