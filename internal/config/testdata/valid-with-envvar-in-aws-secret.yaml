aws:
  key: dummy-key
  secret: "${TEST_ENV_VAR}"
  region: "dummy-region-with-quotation-marks"
  base_endpoint: dummy-base-endpoint

pipeline:
  interval: 3
  callback_url: "dummy-domain.com"

smtp:
  host: dummy-host
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
