attachments:
  base-path: "/base/attachments/path"

callback:
  max_retries: 3
  retry_interval: 5
  url: "dummy-domain.com"

health-check:
  server:
    port: 8080

pipeline:
  interval: 3

unknown-root: "unexpected"

smtp:
  host: dummy-host
  port: 12345
  user: dummy-user
  password: dummy-password
  from: dummy-front
